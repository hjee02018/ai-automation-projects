<UserControl x:Class="EmulatorTrial.Views.PerformMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="320">
    <StackPanel Margin="10">
        <!-- Section: System Resource Monitoring -->
        <TextBlock Text="System Resource Monitoring" FontWeight="Bold" Margin="0,5,0,8" Foreground="#374151"/>
        <Border Background="#F9FAFB" Padding="12" CornerRadius="8" BorderBrush="#E5E7EB" BorderThickness="1">
            <StackPanel>

                <!-- CPU: 배경(Total), 전경(Process) -->
                <DockPanel Margin="0,0,0,4">
                    <TextBlock Text="CPU Usage (Sys/App)" FontSize="11" Foreground="#4B5563"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding ProcessCpuUsage, StringFormat={}{0:F1}%}" FontSize="11" FontWeight="Bold" Foreground="#3B82F6"/>
                        <TextBlock Text=" / " FontSize="11" Foreground="#94a3b8"/>
                        <TextBlock Text="{Binding TotalCpuUsage, StringFormat={}{0:F1}%}" FontSize="11" FontWeight="Bold" Foreground="#94a3b8"/>
                    </StackPanel>
                </DockPanel>
                <Grid Margin="0,0,0,12">
                    <!-- 전체 점유율 (진한 회색/남색 배경) -->
                    <ProgressBar Value="{Binding TotalCpuUsage}" Height="6" Foreground="#cbd5e1" Background="#f1f5f9" BorderThickness="0"/>
                    <!-- 프로세스 점유율 (푸른색 겹침) -->
                    <ProgressBar Value="{Binding ProcessCpuUsage}" Height="6" Foreground="#3B82F6" Background="Transparent" BorderThickness="0"/>
                </Grid>

                <!-- GPU: 배경(Total), 전경(Process) -->
                <DockPanel Margin="0,0,0,4">
                    <TextBlock Text="GPU Usage (Sys/App)" FontSize="11" Foreground="#4B5563"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding ProcessGpuUsage, StringFormat={}{0:F1}%}" FontSize="11" FontWeight="Bold" Foreground="#8B5CF6"/>
                        <TextBlock Text=" / " FontSize="11" Foreground="#c084fc"/>
                        <TextBlock Text="{Binding TotalGpuUsage, StringFormat={}{0:F1}%}" FontSize="11" FontWeight="Bold" Foreground="#c084fc"/>
                    </StackPanel>
                </DockPanel>
                <Grid Margin="0,0,0,12">
                    <ProgressBar Value="{Binding TotalGpuUsage}" Height="6" Foreground="#e9d5ff" Background="#f1f5f9" BorderThickness="0"/>
                    <ProgressBar Value="{Binding ProcessGpuUsage}" Height="6" Foreground="#8B5CF6" Background="Transparent" BorderThickness="0"/>
                </Grid>

                <!-- Memory -->
                <DockPanel Margin="0,0,0,4">
                    <TextBlock Text="Memory Usage" FontSize="11" Foreground="#4B5563"/>
                    <TextBlock Text="{Binding MemoryUsageText}" HorizontalAlignment="Right" FontSize="11" FontWeight="Bold" Foreground="#10B981"/>
                </DockPanel>
                <Grid>
                    <ProgressBar Value="{Binding ProcessMemoryPercentage}" Height="6" Foreground="#10B981" Background="Transparent" BorderThickness="0"/>
                    <ProgressBar Value="{Binding TotalMemoryPercentage}" Height="6" Foreground="#bbf7d0" Background="#f1f5f9" BorderThickness="0"/>
                </Grid>

            </StackPanel>
        </Border>

        <!-- Section: Digital Twin Reliability -->
        <TextBlock Text="Reliability &amp; Fidelity" FontWeight="Bold" Margin="0,15,0,8" Foreground="#374151"/>
        <Border Background="#EEF2FF" Padding="12" CornerRadius="8" BorderBrush="#C7D2FE" BorderThickness="1">
            <StackPanel>
                <DockPanel>
                    <TextBlock Text="DT Fidelity Score" FontSize="12" FontWeight="Bold" Foreground="#4338CA"/>
                    <TextBlock Text="98.2%" HorizontalAlignment="Right" FontSize="14" FontWeight="ExtraBold" Foreground="#4338CA"/>
                </DockPanel>
                <TextBlock Text="Sim vs Real Data Error: 1.8%" FontSize="10" Foreground="#6366F1" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>

        <!-- Section: Network Edge Case Injection -->
        <TextBlock Text="Network Edge Case (Inject)" FontWeight="Bold" Margin="0,15,0,8" Foreground="#374151"/>
        <UniformGrid Columns="2">
            <ToggleButton Content="Latency (200ms)" Margin="2" Padding="5" FontSize="10" Background="White" BorderBrush="#D1D5DB"/>
            <ToggleButton Content="Packet Loss" Margin="2" Padding="5" FontSize="10" Background="White" BorderBrush="#D1D5DB"/>
            <ToggleButton Content="Async Drift" Margin="2" Padding="5" FontSize="10" Background="White" BorderBrush="#D1D5DB"/>
            <ToggleButton Content="Force Offline" Margin="2" Padding="5" FontSize="10" Background="White" BorderBrush="#EF4444" Foreground="#EF4444"/>
        </UniformGrid>
    </StackPanel>
</UserControl>